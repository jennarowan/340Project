<html>

<link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
<title>Edit Liquor</title>

<header>
    <h1>JL Liquors Inc.</h1>
</header>

<body>
    <div>
        <table border="1" cellpadding="5"> 
            <h1> Editing Liquor: </h1>
            {% if liquors %}
                <thead>
                    <tr>
                        <!-- Iterate through each key in the first entry to get the column name -->
                        {% for key in liquors[0].keys() %}

                        <!-- Create a <th> tag with the key inside of it, this will be our header row -->
                        <th>{{ key }}</th>
                        
                        <!-- End of this for loop -->
                        {% endfor %} 
                    </tr>
                
                <tbody>
                    <!-- Now, iterate through every item in data -->
                    {% for liquor in liquors %}
                    <tr>
                        <!-- Then iterate through every key in the current item dictionary -->
                        {% for key in liquor.keys() %}
                       
                        <!-- Create a <td> element with the value of that key in it -->
                        <td>{{liquor[key]}}</td> {% endfor %} 
                    </tr> 
                    {% endfor %} 
            {% endif %} 
        </table>

        <div id ="edit">

            <form id="editLiquor" action="/liquors-edit/{{productID}}/{{productName}}/{{productSizeMl}}/{{productPrice}}" method="post">
                <p></p>
                
                <p></p>
                <fieldset>
                <! -- input type="hidden" value="{{productID}}" class = "form-control" name="productID" id="productID" required -- >
                <p>
                <!--label> ID: </label --> 
                
                <input type="hidden" value="{{productID}}" class = "form-control" name="productID" id="productID" required>
                <p>
                <label>Product Name:</label> <input type="text" value = "{{productName}}" class = "form-control" name="productName" id="productName">
                </p>
                <p>
                <label>Product Size (Ml):</label> <input type="text" value = "{{productSizeMl}}" class = "form-control" name = "productSizeMl" id="productSizeMl">
                </p>
                <p>
                <label>Product Price:</label> <input type="text" value="{{productPrice}}" name="productPrice" id="productPrice" >
                </p>
                <p>
            </fieldset>
                <p></p>
                <input type="submit" value="Edit Liquor" name="Edit_Liquor" class="btn btn-primary" style="margin:.5rem;">
                <a href='/liquors'><input class="btn" type="button" value="Cancel"></a>
            </form> 
        </div>
</body>

<footer class="page-footer">
    <p>© 2022 Liam Maloney and Jenna Rowan</p>
</footer>

</html>