<html>

<link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
<title>Delete Store</title>

<header>
    <h1>JL Liquors Inc.</h1>
</header>

<body>
    <div>
        <form id="deleteStore" action="/stores-delete/{{storeNum}}" method="post">
        <table border="1" cellpadding="5"> 
            <h1> Deleting Store: </h1>
            <!-- Access data tuple passed from app.py -->
            {% if stores %}
                <thead>
                    <tr>
                        <!-- Iterate through each key in the first entry to get the column name -->
                        {% for key in stores[0].keys() %}

                        <!-- Create a <th> tag with the key inside of it, this will be our header row -->
                        <th>{{ key }}</th>
                        
                        <!-- End of this for loop -->
                        {% endfor %} 
                    </tr>

                <tbody>
                    <!-- Now, iterate through every item in data -->
                    {% for store in stores %}
                    <tr>
                        <!-- Then iterate through every key in the current item dictionary -->
                        {% for key in store.keys() %}
                       
                        <!-- Create a <td> element with the value of that key in it -->
                        <td>{{store[key]}}</td> {% endfor %} 
                    </tr> 
                    {% endfor %} 
            {% endif %} 
            <input type="hidden" value="{{storeNum}}" class="form-control" id="storeID" name="storeID" required>
        </table>
        
        <input type="submit" value="Delete Store" name="Delete_Store" class="btn btn-primary" style="margin:.5rem;">
        <a href='/stores'><input class="btn" type="button" value="Cancel"></a>
        </form>
    </div>
</body>

<footer class="page-footer">
    <p>© 2022 Liam Maloney and Jenna Rowan</p>
</footer>

</html>
